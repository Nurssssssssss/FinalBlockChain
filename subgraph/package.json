{
  "name": "defi-superapp-lite-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defi-superapp-lite"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.88.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}
